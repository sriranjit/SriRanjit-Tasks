// Declaring and assigning in two steps

var playerName;
var countryName;

playerName = "Sachin Tendulkar";
countryName = "India";

console.log(playerName + " from " + countryName);

/* Further Adventures
 *
 * 1) Declare a score variable.
 *
 * 2) Assign it a value.
 *
 * 3) Display an extra message on the console,
 *    by combining variables and strings, to say
 *    something like "Lionel Messi has a score of 100"
 *
 */

// 1) Declare a score variable
var score;

// 2) Assign it a value
score =200;

// 3) Display an extra message on the console, combining variables and strings
console.log(playerName + " has scored " + score);
